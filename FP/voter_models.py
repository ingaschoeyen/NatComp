import numpy as np
import matplotlib.pyplot as plt

class Voter():

    def __init__():
        # initialise with random position and strategy
        pass

class Population(Voter):

    def __init__(population_size=100):
        self.N = population_size
        self.initialisePop()
        self.pop_div = 0 # divergence of pop opinion spread
        self.pop_mean = 0 # mean of pop opinion

    def initialisePop():
        self.pop = []
        for n in range(self.N):
            self.pop.append(super.__init__)
        self.pop_div = 
        self.pop_mean = 